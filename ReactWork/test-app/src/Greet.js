

const Greet = props => {
    return (
        <div>
            <h1>
                Hello {props.name} /
                {props.heroName}
            </h1>
        </div>
    )
}
export default Greet;